name: refresh-secrets

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-staging
  cancel-in-progress: false

env:
  AWS_REGION: us-east-1
  HEALTH_URL: https://api.marketing.stg.sidocsa.com/health

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502 # v4.0.2
        with:
          role-to-assume: ${{ vars.GH_OIDC_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: trigger bootstrap restart
        id: ssm
        run: |
          set -euo pipefail
          CMD_ID=$(aws ssm send-command \
            --document-name AWS-RunShellScript \
            --targets "Key=tag:Environment,Values=staging" "Key=tag:Project,Values=sidoc-ai-marketing" \
            --comment "refresh-secrets" \
            --parameters commands='["sudo systemctl restart sidoc-ai-bootstrap.service"]' \
            --output text --query "Command.CommandId")
          echo "command_id=${CMD_ID}" >> "$GITHUB_OUTPUT"

      - name: wait for ssm command
        env:
          CMD_ID: ${{ steps.ssm.outputs.command_id }}
        run: |
          set -euo pipefail
          INSTANCE_ID=$(aws ec2 describe-instances \
            --filters "Name=tag:Environment,Values=staging" "Name=tag:Project,Values=sidoc-ai-marketing" "Name=instance-state-name,Values=running" \
            --query "Reservations[0].Instances[0].InstanceId" --output text)
          for i in $(seq 1 60); do
            STATUS=$(aws ssm get-command-invocation --command-id "${CMD_ID}" --instance-id "${INSTANCE_ID}" --query Status --output text 2>/dev/null || echo Pending)
            echo "[$i] status=${STATUS}"
            case "${STATUS}" in
              Success) exit 0 ;;
              Failed|Cancelled|TimedOut)
                aws ssm get-command-invocation --command-id "${CMD_ID}" --instance-id "${INSTANCE_ID}" --output json
                exit 1 ;;
            esac
            sleep 5
          done
          exit 1

      - name: health check
        run: |
          set -euo pipefail
          for i in $(seq 1 12); do
            CODE=$(curl -s -o /dev/null -w '%{http_code}' "${HEALTH_URL}" || echo 000)
            echo "[$i] /health => ${CODE}"
            if [ "${CODE}" = "200" ]; then exit 0; fi
            sleep 10
          done
          exit 1
