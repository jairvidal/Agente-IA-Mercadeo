# Orchestrator API base URL.
# Dev local:  http://localhost:3001
# Staging:    https://staging.<domain>/api
# Prod:       https://<domain>/api
VITE_API_URL=http://localhost:3001

# Environment label exposed to client code (feature flags, logging).
VITE_ENV=dev
